import type { Metadata } from "next";
import { GPSTrilaterationStudio } from "@/components/studio/GPSTrilaterationStudio";
import { StudioPageShell } from "@/components/studio/StudioPageShell";
export const metadata: Metadata = { title: "GPS Trilateration Simulator (Browser)", description: "See how GPS finds your position by intersecting distance circles from satellites, and how ranging noise blurs the fix. Free.", alternates: { canonical: "/studio/gps-trilateration" } };
export default function Page() { return <StudioPageShell slug="gps-trilateration" name="GPS Trilateration" keyword="GPS trilateration" lede="How does your phone know where it is? By measuring its distance to several satellites and finding the one point consistent with all of them." about="Each satellite&apos;s signal travel time gives the receiver&apos;s distance to it, placing the receiver on a circle — a sphere in three dimensions. Intersecting three or more of these ranges pinpoints the location. With perfect measurements they meet at a point, but clock and atmospheric noise blur the intersection, which is why GPS uses many satellites and least-squares fitting."><GPSTrilaterationStudio /></StudioPageShell>; }
