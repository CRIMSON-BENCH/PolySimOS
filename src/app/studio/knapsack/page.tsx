import type { Metadata } from "next";
import { KnapsackStudio } from "@/components/studio/KnapsackStudio";
import { StudioPageShell } from "@/components/studio/StudioPageShell";
export const metadata: Metadata = { title: "0/1 Knapsack Solver (Dynamic Programming, Browser)", description: "Solve the 0/1 knapsack problem with dynamic programming — pick items to maximize value under a weight limit. Free, interactive.", alternates: { canonical: "/studio/knapsack" } };
export default function Page() { return <StudioPageShell slug="knapsack" name="0/1 Knapsack" keyword="knapsack problem solver" lede="Pack the most valuable haul without overloading the bag. The knapsack problem looks simple but defeats greedy strategies — dynamic programming cracks it." about="Given items with weights and values and a capacity limit, the 0/1 knapsack problem chooses a subset of maximum value. Grabbing the highest-value or best-ratio item first can be badly suboptimal, so the solution builds a table of the best achievable value for every capacity. It models budgeting, cargo loading, and portfolio selection — a classic NP-hard problem tamed by DP."><KnapsackStudio /></StudioPageShell>; }
