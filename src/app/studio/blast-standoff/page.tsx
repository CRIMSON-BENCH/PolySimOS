import type { Metadata } from "next";
import { BlastStandoffStudio } from "@/components/studio/BlastStandoffStudio";
import { StudioPageShell } from "@/components/studio/StudioPageShell";
export const metadata: Metadata = { title: "Blast Overpressure & Standoff Distance Calculator", description: "Estimate explosive blast overpressure and safe standoff distance using cube-root scaling. Free planning tool for EOD and responders.", alternates: { canonical: "/studio/blast-standoff" } };
export default function Page() { return <StudioPageShell slug="blast-standoff" name="Blast Standoff / Overpressure" keyword="blast standoff distance calculator" lede="How far is far enough? Set the charge size and see the overpressure at a given distance, plus the standoff where blast pressure falls to the window-breakage threshold." about="Blast overpressure scales by the Hopkinson-Cranz cube-root law: the scaled distance Z = R/W^(1/3) determines peak pressure, so doubling the charge only extends a given pressure ring by about 26 percent. Colored rings mark injury and damage thresholds. This is a screening estimate for standoff planning and training — always defer to EOD technicians and official safe-distance tables on an actual device."><BlastStandoffStudio /></StudioPageShell>; }
