import type { Metadata } from "next";
import { GeneticDriftStudio } from "@/components/studio/GeneticDriftStudio";
import { StudioPageShell } from "@/components/studio/StudioPageShell";
export const metadata: Metadata = { title: "Genetic Drift Simulator (Wright-Fisher, Browser)", description: "Watch allele frequencies drift randomly to fixation or loss in the Wright-Fisher model. See how population size drives drift. Free.", alternates: { canonical: "/studio/genetic-drift" } };
export default function Page() { return <StudioPageShell slug="genetic-drift" name="Genetic Drift (Wright-Fisher)" keyword="genetic drift simulator" lede="Evolution without selection. Pure chance in sampling each generation makes allele frequencies wander — and in small populations, quickly fix or vanish." about="The Wright-Fisher model builds each generation by randomly sampling alleles from the previous one. With no selection, frequencies still drift because sampling is random, and eventually every allele is either fixed or lost. Drift is powerful in small populations and negligible in large ones — a key force in evolution and conservation genetics. Each line is a replicate population."><GeneticDriftStudio /></StudioPageShell>; }
