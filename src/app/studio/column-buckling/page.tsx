import type { Metadata } from "next";
import { ColumnBucklingStudio } from "@/components/studio/ColumnBucklingStudio";
import { StudioPageShell } from "@/components/studio/StudioPageShell";
export const metadata: Metadata = { title: "Euler Column Buckling Calculator (Browser)", description: "Compute the Euler critical buckling load and slenderness ratio for columns with various end conditions. Free, interactive.", alternates: { canonical: "/studio/column-buckling" } };
export default function Page() { return <StudioPageShell slug="column-buckling" name="Column Buckling (Euler)" keyword="Euler column buckling calculator" lede="A slender column does not crush — it suddenly bows sideways. See how end restraints and slenderness set the Euler critical load at which it buckles." about="Euler&apos;s formula gives the critical buckling load Pcr = π²EI/(KL)², where the effective-length factor K captures the end conditions. Fixing both ends quarters the effective length and quadruples capacity versus a pinned column. Slenderness KL/r decides whether buckling or material yielding governs. Educational tool, not a code-based design."><ColumnBucklingStudio /></StudioPageShell>; }
