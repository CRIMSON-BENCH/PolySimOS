import type { Metadata } from "next";
import { TransistorBiasStudio } from "@/components/studio/TransistorBiasStudio";
import { StudioPageShell } from "@/components/studio/StudioPageShell";
export const metadata: Metadata = { title: "BJT Transistor Bias & Q-Point Calculator (Browser)", description: "Design voltage-divider bias for a BJT and see the Q-point on the DC load line, with region and collector current. Free, interactive.", alternates: { canonical: "/studio/transistor-bias" } };
export default function Page() { return <StudioPageShell slug="transistor-bias" name="BJT Transistor Bias" keyword="BJT bias Q-point calculator" lede="Before a transistor can amplify, it must be biased. Set the resistor network and watch the operating point land on the load line — in the active region, or clipped into saturation." about="Voltage-divider bias fixes a bipolar transistor&apos;s operating point (Q-point) so it stays stable despite variations in current gain β. The base network sets the emitter current, and the collector-emitter voltage falls on the DC load line. A well-designed amplifier places the Q-point near mid-line, giving maximum symmetric swing before clipping."><TransistorBiasStudio /></StudioPageShell>; }
