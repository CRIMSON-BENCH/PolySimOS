import type { Metadata } from "next";
import { MapProjectionStudio } from "@/components/studio/MapProjectionStudio";
import { StudioPageShell } from "@/components/studio/StudioPageShell";
export const metadata: Metadata = { title: "Map Projection Distortion Simulator (Mercator, Browser)", description: "See how the Mercator projection distorts area toward the poles, with Tissot circles. Why Greenland looks huge. Free.", alternates: { canonical: "/studio/map-projection" } };
export default function Page() { return <StudioPageShell slug="map-projection" name="Map Projection Distortion" keyword="Mercator projection distortion" lede="Why does Greenland look as big as Africa when it is fourteen times smaller? Because flattening a sphere onto paper is impossible without distortion." about="Every flat map lies somewhere. The Mercator projection preserves compass angles — invaluable for navigation — but stretches area toward the poles by 1/cos²(latitude). The Tissot indicatrices, all identical circles on the globe, balloon dramatically at high latitudes here, revealing exactly how much size is exaggerated far from the equator."><MapProjectionStudio /></StudioPageShell>; }
