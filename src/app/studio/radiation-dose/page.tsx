import type { Metadata } from "next";
import { RadiationDoseStudio } from "@/components/studio/RadiationDoseStudio";
import { StudioPageShell } from "@/components/studio/StudioPageShell";
export const metadata: Metadata = { title: "Radiation Dose Calculator (Time, Distance, Browser)", description: "Estimate radiation dose from source activity, distance, and time, and compare to common references in millisieverts. Free.", alternates: { canonical: "/studio/radiation-dose" } };
export default function Page() { return <StudioPageShell slug="radiation-dose" name="Radiation Dose" keyword="radiation dose calculator" lede="Time, distance, shielding — the three rules of radiation safety. See how dose builds with exposure and falls with the square of distance." about="Radiation dose accumulates with exposure time but drops with the inverse square of distance, so doubling your distance quarters the dose. Effective dose in millisieverts lets any exposure be compared to everyday references — a dental X-ray, a CT scan, or the annual dose limits for the public and radiation workers. Educational estimate only, not a health-physics assessment."><RadiationDoseStudio /></StudioPageShell>; }
