import type { Metadata } from "next";
import { DiffieHellmanStudio } from "@/components/studio/DiffieHellmanStudio";
import { StudioPageShell } from "@/components/studio/StudioPageShell";
export const metadata: Metadata = { title: "Diffie-Hellman Key Exchange Simulator (Browser)", description: "See how two parties agree on a shared secret over an open channel with Diffie-Hellman key exchange. Free, interactive.", alternates: { canonical: "/studio/diffie-hellman" } };
export default function Page() { return <StudioPageShell slug="diffie-hellman" name="Diffie-Hellman Key Exchange" keyword="Diffie-Hellman key exchange" lede="How do two people agree on a secret while a spy listens to everything they say? Diffie-Hellman — the breakthrough that made public-key cryptography possible." about="Both parties share public numbers, each keeps a private exponent, and they swap the generator raised to those secrets. Raising the other&apos;s public value to your own private exponent gives an identical shared key. An eavesdropper sees all the public values but would need to solve the discrete-logarithm problem to recover the key — computationally infeasible."><DiffieHellmanStudio /></StudioPageShell>; }
