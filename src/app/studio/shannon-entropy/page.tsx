import type { Metadata } from "next";
import { ShannonEntropyStudio } from "@/components/studio/ShannonEntropyStudio";
import { StudioPageShell } from "@/components/studio/StudioPageShell";
export const metadata: Metadata = { title: "Shannon Entropy & Password Strength Calculator (Browser)", description: "Measure the Shannon entropy of a password in bits and estimate its brute-force crack time. Free, interactive.", alternates: { canonical: "/studio/shannon-entropy" } };
export default function Page() { return <StudioPageShell slug="shannon-entropy" name="Shannon Entropy & Passwords" keyword="Shannon entropy password strength" lede="How much information is in a password? Shannon entropy measures its unpredictability in bits — and reveals why length beats a scramble of symbols." about="Shannon entropy quantifies information as the number of yes/no questions needed to determine a value. For a password it is the length times the log₂ of the character-set size, measuring how hard it is to guess. Each additional bit doubles the brute-force effort, so a long passphrase can be far stronger than a short, complex string. The foundation of information theory."><ShannonEntropyStudio /></StudioPageShell>; }
