import type { Metadata } from "next";
import { DifferentialDriveStudio } from "@/components/studio/DifferentialDriveStudio";
import { StudioPageShell } from "@/components/studio/StudioPageShell";
export const metadata: Metadata = { title: "Differential Drive Robot Simulator (Browser)", description: "Drive a differential-drive robot with two wheel speeds and watch the path it traces. Free, interactive robotics tool.", alternates: { canonical: "/studio/differential-drive" } };
export default function Page() { return <StudioPageShell slug="differential-drive" name="Differential Drive Robot" keyword="differential drive robot simulator" lede="Two wheels, endless paths. A differential-drive robot steers like a tank — set each wheel&apos;s speed and watch the trajectory curve, loop, or spin in place." about="A differential-drive robot follows the unicycle kinematic model: its forward speed is the average of the two wheel speeds, and its turn rate is their difference divided by the wheelbase. Equal speeds go straight, unequal speeds arc, and opposite speeds rotate on the spot. The basis of most mobile robots and vacuum bots."><DifferentialDriveStudio /></StudioPageShell>; }
