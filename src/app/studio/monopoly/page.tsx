import type { Metadata } from "next";
import { MonopolyStudio } from "@/components/studio/MonopolyStudio";
import { StudioPageShell } from "@/components/studio/StudioPageShell";
export const metadata: Metadata = { title: "Monopoly Pricing & Deadweight Loss Simulator (Browser)", description: "See how a monopolist sets price where marginal revenue equals marginal cost, and the deadweight loss versus competition. Free.", alternates: { canonical: "/studio/monopoly" } };
export default function Page() { return <StudioPageShell slug="monopoly" name="Monopoly Pricing" keyword="monopoly pricing deadweight loss" lede="With no competitors, a firm restricts output to push up the price. See exactly how a monopoly prices, and the social cost — the deadweight loss — it leaves behind." about="A profit-maximizing monopolist produces where marginal revenue equals marginal cost, then charges the highest price demand will bear. Because marginal revenue lies below the demand curve, output is lower and price higher than in perfect competition, where price equals marginal cost. The gap is deadweight loss: value-creating trades that never happen."><MonopolyStudio /></StudioPageShell>; }
