import type { Metadata } from "next";
import { DividendDiscountStudio } from "@/components/studio/DividendDiscountStudio";
import { StudioPageShell } from "@/components/studio/StudioPageShell";
export const metadata: Metadata = { title: "Dividend Discount Model Calculator (Gordon Growth)", description: "Value a dividend-paying stock with the Gordon growth model from the next dividend, growth rate, and required return. Free, interactive.", alternates: { canonical: "/studio/dividend-discount" } };
export default function Page() { return <StudioPageShell slug="dividend-discount" name="Dividend Discount Model" keyword="gordon growth model calculator" lede="A dividend stock is worth the stream of dividends it will pay. The Gordon growth model prices it in one elegant — and dangerously sensitive — formula." about="The Gordon growth (dividend discount) model values a share as P = D₁/(r − g): next year's dividend divided by the spread between required return and dividend growth. Because the denominator shrinks as growth nears the discount rate, the valuation is extremely sensitive to those inputs. Educational tool, not financial advice."><DividendDiscountStudio /></StudioPageShell>; }
