import type { Metadata } from "next";
import { FissionReactorStudio } from "@/components/studio/FissionReactorStudio";
import { StudioPageShell } from "@/components/studio/StudioPageShell";
export const metadata: Metadata = { title: "Fission Chain Reaction & Criticality Simulator (Browser)", description: "See how the neutron multiplication factor k drives a chain reaction subcritical, critical, or supercritical. Free, interactive.", alternates: { canonical: "/studio/fission-reactor" } };
export default function Page() { return <StudioPageShell slug="fission-reactor" name="Fission Chain Reaction" keyword="fission chain reaction criticality" lede="One number decides whether a pile of uranium sits quiet, powers a city, or runs away: k, the neutron multiplication factor. Watch it play out." about="Each fission emits neutrons that may trigger more fissions. The multiplication factor k is the average number that succeed. Below one, the chain dies; exactly one holds steady power — a critical reactor; above one, the population grows exponentially. Control rods absorb neutrons to keep k balanced at one. Educational model, not an operational simulation."><FissionReactorStudio /></StudioPageShell>; }
