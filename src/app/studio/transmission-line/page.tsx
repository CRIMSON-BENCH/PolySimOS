import type { Metadata } from "next";
import { TransmissionLineStudio } from "@/components/studio/TransmissionLineStudio";
import { StudioPageShell } from "@/components/studio/StudioPageShell";
export const metadata: Metadata = { title: "Transmission Line & VSWR Simulator (Browser)", description: "Visualize reflections and standing waves on a transmission line and compute VSWR and return loss from the load impedance. Free.", alternates: { canonical: "/studio/transmission-line" } };
export default function Page() { return <StudioPageShell slug="transmission-line" name="Transmission Line & VSWR" keyword="transmission line VSWR calculator" lede="Mismatch an antenna and energy bounces back. Watch the incident and reflected waves combine into a standing wave, and see the VSWR every RF engineer chases toward 1:1." about="When a transmission line is terminated in an impedance different from its characteristic impedance, part of the wave reflects. The reflection coefficient sets the voltage standing wave ratio (VSWR) and return loss. A perfect match produces no reflection and VSWR of 1 — the target of antenna tuning and impedance matching."><TransmissionLineStudio /></StudioPageShell>; }
