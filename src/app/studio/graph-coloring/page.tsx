import type { Metadata } from "next";
import { GraphColoringStudio } from "@/components/studio/GraphColoringStudio";
import { StudioPageShell } from "@/components/studio/StudioPageShell";
export const metadata: Metadata = { title: "Graph Coloring Visualizer (Chromatic Number, Browser)", description: "Color a graph so no adjacent nodes share a color, using the greedy algorithm. See the chromatic number. Free, interactive.", alternates: { canonical: "/studio/graph-coloring" } };
export default function Page() { return <StudioPageShell slug="graph-coloring" name="Graph Coloring" keyword="graph coloring chromatic number" lede="Color the map so no two neighbors match, using as few colors as possible. A deceptively hard problem behind scheduling, frequencies, and compilers." about="Graph coloring assigns colors to nodes so no edge joins two of the same color, minimizing the number of colors — the chromatic number. The greedy algorithm colors nodes in order with the lowest available color. It models exam timetabling, radio-frequency assignment, and register allocation, where conflicting items must be kept apart. Optimal coloring is NP-hard."><GraphColoringStudio /></StudioPageShell>; }
