import type { Metadata } from "next";
import { ToleranceStackupStudio } from "@/components/studio/ToleranceStackupStudio";
import { StudioPageShell } from "@/components/studio/StudioPageShell";
export const metadata: Metadata = { title: "Tolerance Stack-Up Calculator (Browser)", description: "Compare worst-case and statistical (RSS) tolerance stack-up for an assembly of parts. Free, interactive engineering tool.", alternates: { canonical: "/studio/tolerance-stackup" } };
export default function Page() { return <StudioPageShell slug="tolerance-stackup" name="Tolerance Stack-Up" keyword="tolerance stack-up calculator" lede="Stack a few parts together and their tolerances add up — but how you add them changes everything, and how much precision each part really needs." about="Worst-case tolerance analysis simply sums all part tolerances, guaranteeing fit but assuming every part hits its extreme at once — rare in practice. Statistical root-sum-square analysis adds tolerances in quadrature for a much tighter, realistic range. Choosing between them balances assembly reliability against the cost of tight manufacturing tolerances."><ToleranceStackupStudio /></StudioPageShell>; }
