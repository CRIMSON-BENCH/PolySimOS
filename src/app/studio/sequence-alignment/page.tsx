import type { Metadata } from "next";
import { SequenceAlignmentStudio } from "@/components/studio/SequenceAlignmentStudio";
import { StudioPageShell } from "@/components/studio/StudioPageShell";
export const metadata: Metadata = { title: "Sequence Alignment Tool (Needleman-Wunsch, Browser)", description: "Globally align two DNA sequences with the Needleman-Wunsch dynamic-programming algorithm. See the scoring matrix and alignment. Free.", alternates: { canonical: "/studio/sequence-alignment" } };
export default function Page() { return <StudioPageShell slug="sequence-alignment" name="Sequence Alignment (Needleman-Wunsch)" keyword="Needleman-Wunsch sequence alignment" lede="How similar are two genes? Type two DNA sequences and watch dynamic programming find their optimal alignment, gaps and all — the bedrock algorithm of bioinformatics." about="Needleman-Wunsch computes the best global alignment of two sequences by filling a scoring matrix with match, mismatch, and gap penalties, then tracing back the highest-scoring path. It guarantees the optimal alignment and underlies tools for comparing genes, proteins, and whole genomes. The heatmap shows the accumulated score matrix and the panel shows the aligned sequences."><SequenceAlignmentStudio /></StudioPageShell>; }
