import type { Metadata } from "next";
import { LineBalancingStudio } from "@/components/studio/LineBalancingStudio";
import { StudioPageShell } from "@/components/studio/StudioPageShell";
export const metadata: Metadata = { title: "Assembly Line Balancing Calculator (Takt Time, Browser)", description: "Compute takt time, minimum stations, and line efficiency for an assembly line from demand. Free, interactive.", alternates: { canonical: "/studio/line-balancing" } };
export default function Page() { return <StudioPageShell slug="line-balancing" name="Assembly Line Balancing" keyword="assembly line balancing takt time" lede="How many workstations does a line need, and how do you split the work? Takt time sets the pace, and balancing packs tasks into stations without overloading any." about="Takt time is the shift length divided by demand — the rhythm at which each unit must be completed to meet the order. Tasks are grouped into stations so no station&apos;s workload exceeds takt. The theoretical minimum number of stations is the total work divided by takt, and balancing efficiency measures how little idle time remains. Rising demand shortens takt and adds stations."><LineBalancingStudio /></StudioPageShell>; }
