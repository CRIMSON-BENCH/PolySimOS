import type { Metadata } from "next";
import { FiberOpticsStudio } from "@/components/studio/FiberOpticsStudio";
import { StudioPageShell } from "@/components/studio/StudioPageShell";
export const metadata: Metadata = { title: "Fiber Optics Simulator (Total Internal Reflection)", description: "Explore how optical fibers trap light by total internal reflection, with critical angle, numerical aperture, and mode count. Free.", alternates: { canonical: "/studio/fiber-optics" } };
export default function Page() { return <StudioPageShell slug="fiber-optics" name="Fiber Optics" keyword="fiber optics total internal reflection" lede="How does light travel hundreds of kilometers down a glass thread without leaking out? Total internal reflection traps it in the core, bounce after bounce." about="An optical fiber has a high-index core inside a lower-index cladding. Light striking the boundary above the critical angle is completely reflected, guiding it along the fiber. The numerical aperture sets the acceptance cone of usable input angles, and the core diameter decides whether the fiber is single-mode for long-haul telecom or multimode for short links."><FiberOpticsStudio /></StudioPageShell>; }
