import type { Metadata } from "next";
import { CardiacActionPotentialStudio } from "@/components/studio/CardiacActionPotentialStudio";
import { StudioPageShell } from "@/components/studio/StudioPageShell";
export const metadata: Metadata = { title: "Cardiac Action Potential Simulator (Browser)", description: "Visualize the phases of a cardiac action potential — upstroke, plateau, and repolarization — and how heart rate changes them. Free, interactive.", alternates: { canonical: "/studio/cardiac-action-potential" } };
export default function Page() { return <StudioPageShell slug="cardiac-action-potential" name="Cardiac Action Potential" keyword="cardiac action potential simulator" lede="Every heartbeat begins as an electrical spike inside heart-muscle cells. Watch the distinctive plateau that sets heart muscle apart from nerve." about="A cardiac action potential has a fast sodium upstroke, a long calcium plateau, and potassium-driven repolarization. The plateau — absent in nerve cells — keeps the muscle contracted through the whole squeeze and creates a long refractory period that prevents tetanus. Educational tool, not medical advice."><CardiacActionPotentialStudio /></StudioPageShell>; }
