import type { Metadata } from "next";
import { ComparativeAdvantageStudio } from "@/components/studio/ComparativeAdvantageStudio";
import { StudioPageShell } from "@/components/studio/StudioPageShell";
export const metadata: Metadata = { title: "Comparative Advantage & Gains from Trade (Browser)", description: "See Ricardo's comparative advantage with production-possibility frontiers and opportunity cost, and why both countries gain from trade. Free.", alternates: { canonical: "/studio/comparative-advantage" } };
export default function Page() { return <StudioPageShell slug="comparative-advantage" name="Comparative Advantage" keyword="comparative advantage gains from trade" lede="Even if one country is worse at making everything, trade still makes both better off. Ricardo&apos;s counterintuitive result, made visible." about="Comparative advantage compares opportunity costs, not raw productivity. Each country should specialize in the good it sacrifices the least to produce — the flatter or steeper slope of its production-possibility frontier. Trading at a price between the two opportunity costs lets both countries consume beyond their own frontiers. It is the foundation of international trade theory."><ComparativeAdvantageStudio /></StudioPageShell>; }
