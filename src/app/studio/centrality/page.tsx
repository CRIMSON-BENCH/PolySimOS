import type { Metadata } from "next";
import { CentralityStudio } from "@/components/studio/CentralityStudio";
import { StudioPageShell } from "@/components/studio/StudioPageShell";
export const metadata: Metadata = { title: "Network Centrality Visualizer (Browser)", description: "Compare degree, betweenness, and closeness centrality to find the most important nodes in a network. Free, interactive.", alternates: { canonical: "/studio/centrality" } };
export default function Page() { return <StudioPageShell slug="centrality" name="Network Centrality" keyword="network centrality betweenness" lede="Who is the most important person in the network? The answer depends on what you mean — the most connected, the best-positioned bridge, or the closest to everyone." about="Centrality quantifies node importance in different ways. Degree centrality counts direct links; betweenness counts how often a node sits on shortest paths, marking brokers and bottlenecks; closeness measures how quickly a node can reach the whole network. Switching metrics can crown entirely different nodes — crucial for social-network analysis, epidemiology, and infrastructure resilience."><CentralityStudio /></StudioPageShell>; }
