import type { Metadata } from "next";
import { IndifferenceCurveStudio } from "@/components/studio/IndifferenceCurveStudio";
import { StudioPageShell } from "@/components/studio/StudioPageShell";
export const metadata: Metadata = { title: "Consumer Choice & Indifference Curves Simulator (Browser)", description: "Explore consumer choice — indifference curves, the budget line, and the utility-maximizing bundle where they touch. Free, interactive.", alternates: { canonical: "/studio/indifference-curves" } };
export default function Page() { return <StudioPageShell slug="indifference-curves" name="Consumer Choice" keyword="indifference curves consumer choice" lede="How does a shopper spend a fixed budget to be happiest? At the sweet spot where the budget line just kisses the highest reachable indifference curve." about="Indifference curves connect bundles of two goods that give equal satisfaction; higher curves mean more utility. The budget line shows every affordable bundle. The optimal choice is the tangency point, where the marginal rate of substitution equals the price ratio. Changing a price or income pivots the budget line and shifts the optimum — the core model of consumer demand."><IndifferenceCurveStudio /></StudioPageShell>; }
