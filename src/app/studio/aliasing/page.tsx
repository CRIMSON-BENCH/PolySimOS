import type { Metadata } from "next";
import { AliasingStudio } from "@/components/studio/AliasingStudio";
import { StudioPageShell } from "@/components/studio/StudioPageShell";
export const metadata: Metadata = { title: "Sampling & Aliasing Simulator (Nyquist, Browser)", description: "See the Nyquist sampling theorem in action — sample a signal too slowly and watch aliasing turn it into a false lower frequency. Free.", alternates: { canonical: "/studio/aliasing" } };
export default function Page() { return <StudioPageShell slug="aliasing" name="Sampling & Aliasing" keyword="sampling aliasing Nyquist" lede="Why do wagon wheels spin backward in films? Sample a signal below the Nyquist rate and a high frequency disguises itself as a low one — the trap of aliasing." about="The Nyquist-Shannon sampling theorem says you must sample above twice a signal&apos;s highest frequency to reconstruct it faithfully. Sample too slowly and the reconstructed waveform folds down to a false, lower frequency — aliasing. Push the signal past half the sample rate here to watch the cyan reconstruction collapse. The foundation of all digital audio and imaging."><AliasingStudio /></StudioPageShell>; }
