import type { Metadata } from "next";
import { SkidToStopStudio } from "@/components/studio/SkidToStopStudio";
import { StudioPageShell } from "@/components/studio/StudioPageShell";
export const metadata: Metadata = { title: "Skid-to-Stop Speed Calculator (Accident Reconstruction)", description: "Estimate a vehicle's minimum speed from skid-mark length, drag factor, braking efficiency, and grade. Free reconstruction tool.", alternates: { canonical: "/studio/skid-to-stop" } };
export default function Page() { return <StudioPageShell slug="skid-to-stop" name="Skid-to-Stop Speed" keyword="skid to stop speed calculator" lede="How fast was the vehicle going before it braked? Enter the skid length, road surface, and grade to estimate the minimum speed at the start of the skid." about="Accident reconstruction uses the skid-to-stop equation v = √(30·D·f·n), where D is skid distance in feet, f the surface drag factor, and n braking efficiency. Grade adjusts the effective drag factor. This is an estimation aid for training and preliminary analysis — a court-admissible reconstruction requires scene measurement and a measured drag factor."><SkidToStopStudio /></StudioPageShell>; }
