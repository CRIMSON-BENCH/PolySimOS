import type { Metadata } from "next";
import { SimulatedAnnealingStudio } from "@/components/studio/SimulatedAnnealingStudio";
import { StudioPageShell } from "@/components/studio/StudioPageShell";
export const metadata: Metadata = { title: "Simulated Annealing Optimizer (Browser)", description: "Watch simulated annealing escape local minima and converge to the global optimum as the temperature cools. Free, interactive.", alternates: { canonical: "/studio/simulated-annealing" } };
export default function Page() { return <StudioPageShell slug="simulated-annealing" name="Simulated Annealing" keyword="simulated annealing optimizer" lede="Borrowed from how metal cools into a strong crystal, simulated annealing wanders freely when hot and settles into the best valley as it cools." about="Simulated annealing minimizes a rugged objective by accepting worse moves with probability exp(−ΔE/T). At high temperature it roams across the landscape, jumping out of local minima; as the temperature cools, it commits to the best region it found. Cooling too fast traps it early; cooling slowly lets it reach the global optimum — a powerful, general-purpose optimizer."><SimulatedAnnealingStudio /></StudioPageShell>; }
