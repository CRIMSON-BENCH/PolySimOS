import type { Metadata } from "next";
import { ReactorKineticsStudio } from "@/components/studio/ReactorKineticsStudio";
import { StudioPageShell } from "@/components/studio/StudioPageShell";
export const metadata: Metadata = { title: "Reactor Point Kinetics Simulator (Browser)", description: "Explore reactor power response to reactivity with delayed neutrons, reactor period, and prompt criticality. Free, interactive.", alternates: { canonical: "/studio/reactor-kinetics" } };
export default function Page() { return <StudioPageShell slug="reactor-kinetics" name="Reactor Point Kinetics" keyword="reactor point kinetics" lede="Why can operators control a nuclear reactor at all, when neutrons live only milliseconds? The secret is delayed neutrons — and the point-kinetics equations reveal it." about="Point reactor kinetics tracks power against a step change in reactivity. A small fraction of neutrons emerge with a delay of seconds, stretching the response to a controllable reactor period. But if reactivity exceeds the delayed fraction β, the reactor goes prompt-critical and power surges on the millisecond neutron lifetime — the runaway regime operators must never enter. Educational model."><ReactorKineticsStudio /></StudioPageShell>; }
