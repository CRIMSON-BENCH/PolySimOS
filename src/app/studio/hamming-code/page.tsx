import type { Metadata } from "next";
import { HammingStudio } from "@/components/studio/HammingStudio";
import { StudioPageShell } from "@/components/studio/StudioPageShell";
export const metadata: Metadata = { title: "Hamming Error-Correcting Code Simulator (Browser)", description: "See how a Hamming(7,4) code detects and corrects a single-bit error using parity and a syndrome. Free, interactive.", alternates: { canonical: "/studio/hamming-code" } };
export default function Page() { return <StudioPageShell slug="hamming-code" name="Hamming Error Correction" keyword="Hamming code error correction" lede="What if data could fix itself? A Hamming code adds a few parity bits so a single flipped bit is not just detected but pinpointed and corrected automatically." about="The Hamming(7,4) code protects four data bits with three parity bits, each checking an overlapping subset. After transmission, recomputing the parities yields a syndrome whose binary value is exactly the position of the corrupted bit — or zero if none. Error-correcting codes like this safeguard computer memory, deep-space communications, and QR codes against noise."><HammingStudio /></StudioPageShell>; }
