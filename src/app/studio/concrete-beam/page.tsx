import type { Metadata } from "next";
import { ConcreteBeamStudio } from "@/components/studio/ConcreteBeamStudio";
import { StudioPageShell } from "@/components/studio/StudioPageShell";
export const metadata: Metadata = { title: "Reinforced Concrete Beam Capacity Calculator (ACI)", description: "Compute the flexural moment capacity of a reinforced concrete beam using the Whitney stress block. Free, interactive.", alternates: { canonical: "/studio/concrete-beam" } };
export default function Page() { return <StudioPageShell slug="concrete-beam" name="Reinforced Concrete Beam" keyword="reinforced concrete beam capacity" lede="How much moment can this concrete beam carry? Set the section, steel, and material strengths to see the compression block form and the capacity compute." about="A reinforced concrete beam in bending balances a compression block in the concrete against tension in the steel. Equating those forces gives the stress-block depth a, and the nominal moment capacity is Mn = As·fy·(d − a/2). A tension-controlled section, where the steel yields before the concrete crushes, fails in the ductile manner codes require. Educational tool, not a design."><ConcreteBeamStudio /></StudioPageShell>; }
