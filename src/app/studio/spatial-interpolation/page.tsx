import type { Metadata } from "next";
import { SpatialInterpolationStudio } from "@/components/studio/SpatialInterpolationStudio";
import { StudioPageShell } from "@/components/studio/StudioPageShell";
export const metadata: Metadata = { title: "Spatial Interpolation (IDW) Simulator (Browser)", description: "See inverse-distance-weighted interpolation fill a continuous surface from scattered sample points. Free, interactive GIS.", alternates: { canonical: "/studio/spatial-interpolation" } };
export default function Page() { return <StudioPageShell slug="spatial-interpolation" name="Spatial Interpolation (IDW)" keyword="inverse distance weighting interpolation" lede="You have rainfall readings at a dozen gauges — what about everywhere in between? Inverse-distance weighting fills the gaps into a smooth map." about="Inverse-distance weighting estimates a value at any location as a weighted average of nearby samples, with weights falling off as distance to a power. A low power gives smooth, gently blended surfaces; a high power lets each sample dominate its neighborhood, producing bull&apos;s-eyes. It is a staple of GIS for mapping rainfall, pollution, elevation, and mineral grades."><SpatialInterpolationStudio /></StudioPageShell>; }
