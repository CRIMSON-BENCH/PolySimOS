import type { Metadata } from "next";
import { RadiationShieldingStudio } from "@/components/studio/RadiationShieldingStudio";
import { StudioPageShell } from "@/components/studio/StudioPageShell";
export const metadata: Metadata = { title: "Radiation Shielding Calculator (Half-Value Layer, Browser)", description: "Compute gamma-ray attenuation and half-value layer for water, concrete, iron, and lead shields. Free, interactive.", alternates: { canonical: "/studio/radiation-shielding" } };
export default function Page() { return <StudioPageShell slug="radiation-shielding" name="Radiation Shielding" keyword="radiation shielding half-value layer" lede="How much lead stops a gamma ray? Not a hard wall but an exponential fade — see how thickness and material set what gets through." about="Gamma radiation is attenuated exponentially through matter, I = I₀·e^(−μx). The half-value layer is the thickness that halves intensity, and about ten of them reduce it to a thousandth. Dense, high-atomic-number materials like lead have the largest attenuation coefficient, making them the most compact shields. A conceptual tool, not a radiation-protection design."><RadiationShieldingStudio /></StudioPageShell>; }
