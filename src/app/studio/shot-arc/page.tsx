import type { Metadata } from "next";
import { ShotArcStudio } from "@/components/studio/ShotArcStudio";
import { StudioPageShell } from "@/components/studio/StudioPageShell";
export const metadata: Metadata = { title: "Basketball Shot Arc Simulator (Browser)", description: "Explore the physics of a basketball shot — launch angle, entry angle, and the ideal arc for making the basket. Free, interactive.", alternates: { canonical: "/studio/shot-arc" } };
export default function Page() { return <StudioPageShell slug="shot-arc" name="Basketball Shot Arc" keyword="basketball shot arc physics" lede="A great shooter feels the perfect arc. The physics behind it: the ball must drop into the hoop steeply enough to see a wide enough opening — but not so steep it needs a moonball." about="A basketball must enter the rim from above to have room to fall through. Too flat an arc meets the front of the rim; too steep wastes energy and control. An entry angle around 43–47° maximizes the effective target the ball sees. A higher release point lets a shooter use a flatter, more repeatable arc while still scoring — the geometry every jump shot obeys."><ShotArcStudio /></StudioPageShell>; }
