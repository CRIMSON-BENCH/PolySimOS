import type { Metadata } from "next";
import { GraphShortestPathStudio } from "@/components/studio/GraphShortestPathStudio";
import { StudioPageShell } from "@/components/studio/StudioPageShell";
export const metadata: Metadata = { title: "Dijkstra Shortest Path Visualizer (Graph, Browser)", description: "Find shortest paths in a weighted graph with Dijkstra's algorithm. Click a destination and see the route and distances. Free.", alternates: { canonical: "/studio/shortest-path" } };
export default function Page() { return <StudioPageShell slug="shortest-path" name="Shortest Path (Dijkstra)" keyword="Dijkstra shortest path" lede="The routing algorithm inside every GPS. Dijkstra&apos;s method finds the cheapest path through a weighted network, one nearest node at a time." about="Dijkstra&apos;s algorithm computes the shortest distance from a source to every other node in a graph with non-negative edge weights, always finalizing the closest unvisited node next. It underlies GPS navigation, internet routing protocols, and logistics planning. The distances label each node and the highlighted edges trace the optimal route."><GraphShortestPathStudio /></StudioPageShell>; }
