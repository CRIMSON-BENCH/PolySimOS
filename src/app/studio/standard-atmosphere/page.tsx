import type { Metadata } from "next";
import { StandardAtmosphereStudio } from "@/components/studio/StandardAtmosphereStudio";
import { StudioPageShell } from "@/components/studio/StudioPageShell";
export const metadata: Metadata = { title: "Standard Atmosphere Calculator (ISA, Browser)", description: "Compute temperature, pressure, and density versus altitude with the International Standard Atmosphere. Free, interactive.", alternates: { canonical: "/studio/standard-atmosphere" } };
export default function Page() { return <StudioPageShell slug="standard-atmosphere" name="Standard Atmosphere (ISA)" keyword="standard atmosphere calculator" lede="How thin is the air up there? The International Standard Atmosphere is the reference every altimeter, airspeed indicator, and engine is calibrated against." about="The ISA models temperature falling at 6.5 °C/km through the troposphere, then holding constant in the stratosphere above the 11 km tropopause. Pressure and density decay nearly exponentially — at airliner cruise the air is only a quarter as dense as at sea level, which sets true airspeed, lift, and engine performance."><StandardAtmosphereStudio /></StudioPageShell>; }
