import type { Metadata } from "next";
import { TournamentStudio } from "@/components/studio/TournamentStudio";
import { StudioPageShell } from "@/components/studio/StudioPageShell";
export const metadata: Metadata = { title: "Tournament Bracket Odds Simulator (Browser)", description: "Simulate a single-elimination bracket to find each seed's championship probability. See why upsets are so common. Free.", alternates: { canonical: "/studio/tournament-bracket" } };
export default function Page() { return <StudioPageShell slug="tournament-bracket" name="Tournament Bracket Odds" keyword="tournament bracket odds" lede="Why do brackets bust every March? Because even the best team must win several games in a row, and each is a fresh chance to be knocked out." about="A single-elimination tournament punishes any slip. Simulating thousands of runs of the bracket, with each game decided by the seeds&apos; relative strength, reveals the true championship probability for every seed. When the field is evenly matched the odds spread wide; even a dominant favorite rarely exceeds a coin flip to win it all — the mathematics of March Madness."><TournamentStudio /></StudioPageShell>; }
