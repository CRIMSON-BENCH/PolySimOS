import type { Metadata } from "next";
import { PhotoelectricStudio } from "@/components/studio/PhotoelectricStudio";
import { StudioPageShell } from "@/components/studio/StudioPageShell";
export const metadata: Metadata = { title: "Photoelectric Effect Simulator (Browser)", description: "Explore Einstein's photoelectric effect — photon energy, work function, threshold frequency, and stopping voltage. Free, interactive.", alternates: { canonical: "/studio/photoelectric" } };
export default function Page() { return <StudioPageShell slug="photoelectric" name="Photoelectric Effect" keyword="photoelectric effect simulator" lede="Light knocks electrons out of metal — but only if its frequency is high enough, no matter the brightness. The clue that light comes in quantized photons." about="Einstein explained the photoelectric effect with KEmax = hf − φ: each photon delivers energy hf, and an electron escapes only if that exceeds the work function φ. Below a threshold frequency nothing happens; above it, the ejected electrons&apos; maximum kinetic energy rises linearly with frequency, independent of intensity. The result launched quantum physics."><PhotoelectricStudio /></StudioPageShell>; }
