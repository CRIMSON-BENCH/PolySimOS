import type { Metadata } from "next";
import { NeutronTransportStudio } from "@/components/studio/NeutronTransportStudio";
import { StudioPageShell } from "@/components/studio/StudioPageShell";
export const metadata: Metadata = { title: "Neutron Transport & Moderation Simulator (Browser)", description: "Watch neutrons random-walk, scatter, slow down, and escape or absorb — the physics of mean free path and moderation. Free.", alternates: { canonical: "/studio/neutron-transport" } };
export default function Page() { return <StudioPageShell slug="neutron-transport" name="Neutron Transport" keyword="neutron transport moderation" lede="Neutrons do not fly straight — they stumble through matter, bouncing and slowing until they fission, get absorbed, or leak out. That random walk sets a reactor&apos;s fate." about="A neutron scatters roughly every mean free path, losing energy at each collision. This moderation slows fast neutrons to thermal speeds where they fission efficiently. A short mean free path keeps neutrons inside the fuel; a long one lets them leak away, which is why the size and geometry of a reactor or bomb core determine whether a chain reaction can sustain itself."><NeutronTransportStudio /></StudioPageShell>; }
