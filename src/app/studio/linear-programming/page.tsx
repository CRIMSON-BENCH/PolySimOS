import type { Metadata } from "next";
import { LinearProgrammingStudio } from "@/components/studio/LinearProgrammingStudio";
import { StudioPageShell } from "@/components/studio/StudioPageShell";
export const metadata: Metadata = { title: "Linear Programming Visualizer (Browser)", description: "See linear programming solve a 2-variable optimization — feasible region, objective line, and the optimal vertex. Free, interactive.", alternates: { canonical: "/studio/linear-programming" } };
export default function Page() { return <StudioPageShell slug="linear-programming" name="Linear Programming" keyword="linear programming visualizer" lede="The optimization engine behind supply chains and airline scheduling. Constraints carve out a region, and the best answer always hides at a corner." about="Linear programming maximizes a linear objective subject to linear inequality constraints. Those constraints define a convex feasible region, and a fundamental theorem guarantees the optimum lies at one of its vertices. Sliding the objective coefficients moves the optimal corner — the geometric intuition behind the simplex method used across operations research."><LinearProgrammingStudio /></StudioPageShell>; }
