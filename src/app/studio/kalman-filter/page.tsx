import type { Metadata } from "next";
import { KalmanFilterStudio } from "@/components/studio/KalmanFilterStudio";
import { StudioPageShell } from "@/components/studio/StudioPageShell";
export const metadata: Metadata = { title: "Kalman Filter Simulator (State Estimation, Browser)", description: "See a 1D Kalman filter fuse a motion model with noisy measurements to track a moving object. Tune process and measurement noise. Free.", alternates: { canonical: "/studio/kalman-filter" } };
export default function Page() { return <StudioPageShell slug="kalman-filter" name="Kalman Filter" keyword="Kalman filter simulator" lede="The math behind GPS, radar, and spacecraft navigation. A Kalman filter blends a prediction with a noisy measurement to track a hidden state better than either alone." about="The Kalman filter alternates predict and update steps, maintaining an estimate and its uncertainty. The Kalman gain optimally weights the new measurement against the model prediction — trusting the sensor more when the model is uncertain, and vice versa. It turns a jittery sensor stream into a smooth, accurate track, the workhorse of estimation and sensor fusion."><KalmanFilterStudio /></StudioPageShell>; }
