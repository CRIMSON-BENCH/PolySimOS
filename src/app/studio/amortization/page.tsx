import type { Metadata } from "next";
import { AmortizationStudio } from "@/components/studio/AmortizationStudio";
import { StudioPageShell } from "@/components/studio/StudioPageShell";
export const metadata: Metadata = { title: "Loan & Mortgage Amortization Calculator (Browser)", description: "Calculate monthly payment and total interest on a loan, and see how each payment splits between principal and interest. Free.", alternates: { canonical: "/studio/amortization" } };
export default function Page() { return <StudioPageShell slug="amortization" name="Loan / Mortgage Amortization" keyword="mortgage amortization calculator" lede="Your first mortgage payment is almost all interest. See exactly how the principal-and-interest split flips over the life of a loan — and why extra early payments pay off." about="A fixed-rate loan is repaid with a level monthly payment computed from the amount, rate, and term. Early payments are mostly interest because the balance is largest; as the balance falls, more of each payment goes to principal. The chart shows this crossover and the total interest paid. Educational tool, not financial advice."><AmortizationStudio /></StudioPageShell>; }
