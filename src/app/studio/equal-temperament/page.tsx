import type { Metadata } from "next";
import { EqualTemperamentStudio } from "@/components/studio/EqualTemperamentStudio";
import { StudioPageShell } from "@/components/studio/StudioPageShell";
export const metadata: Metadata = { title: "Equal Temperament vs Just Intonation (Browser)", description: "Compare equal-tempered and just-intonation tuning and see the cents deviation of every interval. Free, interactive music theory.", alternates: { canonical: "/studio/equal-temperament" } };
export default function Page() { return <StudioPageShell slug="equal-temperament" name="Equal Temperament vs Just Intonation" keyword="equal temperament just intonation" lede="Your piano is deliberately out of tune. Equal temperament trades perfectly pure intervals for the freedom to play in every key — see exactly what it costs." about="Just intonation tunes intervals to pure whole-number frequency ratios that sound flawlessly consonant, but only in one key. Equal temperament splits the octave into twelve identical semitones so every key is equally usable, leaving each interval except the octave slightly detuned. The chart shows that deviation in cents — a hundredth of a semitone."><EqualTemperamentStudio /></StudioPageShell>; }
