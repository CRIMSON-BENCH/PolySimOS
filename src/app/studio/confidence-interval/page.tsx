import type { Metadata } from "next";
import { ConfidenceIntervalStudio } from "@/components/studio/ConfidenceIntervalStudio";
import { StudioPageShell } from "@/components/studio/StudioPageShell";
export const metadata: Metadata = { title: "Confidence Interval Simulator (Coverage, Browser)", description: "See what a 95% confidence interval really means by drawing many samples and watching which intervals contain the true mean. Free.", alternates: { canonical: "/studio/confidence-interval" } };
export default function Page() { return <StudioPageShell slug="confidence-interval" name="Confidence Intervals" keyword="confidence interval simulator" lede="The most misunderstood idea in statistics, finally made intuitive. Draw fifty samples, build fifty intervals, and watch about 95% of them capture the true mean." about="A confidence interval does not say the true value has a 95% chance of lying in one particular interval. It says the procedure produces intervals that, over many repetitions, contain the true parameter about 95% of the time. The red intervals here are the unlucky ones that miss — exactly the fraction the confidence level predicts."><ConfidenceIntervalStudio /></StudioPageShell>; }
