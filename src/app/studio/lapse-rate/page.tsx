import type { Metadata } from "next";
import { LapseRateStudio } from "@/components/studio/LapseRateStudio";
import { StudioPageShell } from "@/components/studio/StudioPageShell";
export const metadata: Metadata = { title: "Atmospheric Lapse Rate & Cloud Base Calculator", description: "Plot the temperature and pressure profile of the atmosphere and find the cloud base (lifting condensation level). Free, interactive.", alternates: { canonical: "/studio/lapse-rate" } };
export default function Page() { return <StudioPageShell slug="lapse-rate" name="Atmospheric Lapse Rate & Cloud Base" keyword="atmospheric lapse rate cloud base" lede="Why do clouds have flat bottoms all at the same height? Rising air cools until it hits its dew point — the lifting condensation level — and that is where the cloud begins." about="Air temperature falls with altitude at the environmental lapse rate. A rising parcel cools until it reaches saturation at the lifting condensation level, estimated by (T − Td)/8 km, which sets the cloud base. Pressure decreases with height following the barometric formula. Core concepts for meteorology, aviation, and fire-weather forecasting."><LapseRateStudio /></StudioPageShell>; }
