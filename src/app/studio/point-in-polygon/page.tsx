import type { Metadata } from "next";
import { PointInPolygonStudio } from "@/components/studio/PointInPolygonStudio";
import { StudioPageShell } from "@/components/studio/StudioPageShell";
export const metadata: Metadata = { title: "Point-in-Polygon / Geofence Simulator (Browser)", description: "See the ray-casting algorithm decide whether a point is inside a polygon — the math behind geofencing. Free, interactive.", alternates: { canonical: "/studio/point-in-polygon" } };
export default function Page() { return <StudioPageShell slug="point-in-polygon" name="Point in Polygon (Geofence)" keyword="point in polygon ray casting" lede="Is that phone inside the delivery zone? The ray-casting test answers instantly — shoot a ray, count the crossings, and odd means inside." about="The point-in-polygon test casts a ray from the point in any direction and counts how many polygon edges it crosses. An odd count means the point is inside, an even count outside — a beautifully simple result of topology. It powers geofencing (alerting when a device enters or leaves a region), map region selection, and hit-testing in graphics. Click to move the test point."><PointInPolygonStudio /></StudioPageShell>; }
