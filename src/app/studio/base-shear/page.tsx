import type { Metadata } from "next";
import { BaseShearStudio } from "@/components/studio/BaseShearStudio";
import { StudioPageShell } from "@/components/studio/StudioPageShell";
export const metadata: Metadata = { title: "Seismic Base Shear Calculator (Equivalent Lateral Force)", description: "Estimate earthquake base shear and its vertical distribution up a building with the equivalent lateral force method. Free, interactive.", alternates: { canonical: "/studio/base-shear" } };
export default function Page() { return <StudioPageShell slug="base-shear" name="Seismic Base Shear" keyword="seismic base shear calculator" lede="How much sideways force does an earthquake put on a building? The equivalent lateral force method boils it down to a base shear and spreads it up the floors." about="The equivalent lateral force procedure estimates total seismic base shear as V = Cs·W, where the seismic response coefficient Cs scales the design spectral acceleration by the structure&apos;s ductility factor R and importance factor I. The base shear is then distributed up the height, concentrating demand near the roof. Educational tool, not a code design."><BaseShearStudio /></StudioPageShell>; }
