import type { Metadata } from "next";
import { GeostrophicWindStudio } from "@/components/studio/GeostrophicWindStudio";
import { StudioPageShell } from "@/components/studio/StudioPageShell";
export const metadata: Metadata = { title: "Geostrophic Wind Calculator (Browser)", description: "See how the geostrophic wind balances pressure gradient against Coriolis force and blows parallel to the isobars. Free, interactive.", alternates: { canonical: "/studio/geostrophic-wind" } };
export default function Page() { return <StudioPageShell slug="geostrophic-wind" name="Geostrophic Wind" keyword="geostrophic wind calculator" lede="Upper-level wind does not blow from high to low pressure — it runs along the isobars. That is the geostrophic balance you can read right off a weather map." about="High above the friction of the surface, the pressure-gradient force pushing air toward low pressure is balanced by the Coriolis force deflecting it. The result, the geostrophic wind, blows parallel to the isobars, with speed set by how tightly they are packed and by latitude. It is the foundation of reading and forecasting large-scale weather."><GeostrophicWindStudio /></StudioPageShell>; }
