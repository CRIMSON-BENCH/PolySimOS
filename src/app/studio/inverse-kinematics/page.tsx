import type { Metadata } from "next";
import { InverseKinematicsStudio } from "@/components/studio/InverseKinematicsStudio";
import { StudioPageShell } from "@/components/studio/StudioPageShell";
export const metadata: Metadata = { title: "Inverse Kinematics Solver (2-Link Arm, Browser)", description: "Click a target and watch a 2-link robot arm solve inverse kinematics to reach it, with elbow-up and elbow-down solutions. Free.", alternates: { canonical: "/studio/inverse-kinematics" } };
export default function Page() { return <StudioPageShell slug="inverse-kinematics" name="Inverse Kinematics" keyword="inverse kinematics solver" lede="Point at where you want the hand, and solve for the joints. Inverse kinematics is the hard, essential direction behind every robot that reaches for something." about="Inverse kinematics finds the joint angles that place the end effector at a desired target. For a two-link arm the law of cosines gives a closed-form solution — but with a twist: there are usually two configurations (elbow-up and elbow-down), and none when the target lies outside the reachable workspace. Click to move the target."><InverseKinematicsStudio /></StudioPageShell>; }
