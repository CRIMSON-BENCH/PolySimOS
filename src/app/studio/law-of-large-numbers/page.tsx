import type { Metadata } from "next";
import { LawLargeNumbersStudio } from "@/components/studio/LawLargeNumbersStudio";
import { StudioPageShell } from "@/components/studio/StudioPageShell";
export const metadata: Metadata = { title: "Law of Large Numbers Simulator (Browser)", description: "Watch the proportion of coin-flip heads converge to the true probability as flips accumulate. Free, interactive.", alternates: { canonical: "/studio/law-of-large-numbers" } };
export default function Page() { return <StudioPageShell slug="law-of-large-numbers" name="Law of Large Numbers" keyword="law of large numbers" lede="A handful of coin flips is chaos; a million is clockwork. The law of large numbers is why averages are trustworthy even though each flip is pure chance." about="As the number of trials grows, the observed average converges to the true expected value — the foundation of statistics, insurance, and casinos. It is often misread as implying a run of tails makes heads &quot;due,&quot; but each flip stays independent; that belief is the gambler&apos;s fallacy. Convergence comes from swamping early noise with sheer volume, not from any self-correction."><LawLargeNumbersStudio /></StudioPageShell>; }
