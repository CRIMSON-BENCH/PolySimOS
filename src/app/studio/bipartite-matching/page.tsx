import type { Metadata } from "next";
import { BipartiteMatchingStudio } from "@/components/studio/BipartiteMatchingStudio";
import { StudioPageShell } from "@/components/studio/StudioPageShell";
export const metadata: Metadata = { title: "Bipartite Matching Visualizer (Assignment, Browser)", description: "Find the maximum bipartite matching — assign workers to jobs optimally with augmenting paths. Free, interactive.", alternates: { canonical: "/studio/bipartite-matching" } };
export default function Page() { return <StudioPageShell slug="bipartite-matching" name="Bipartite Matching" keyword="bipartite matching assignment" lede="Pair people to tasks, students to schools, donors to recipients — as many valid matches as possible, none double-booked. The assignment problem, solved." about="Maximum bipartite matching finds the largest set of pairings between two groups such that each valid edge respects the constraints and no node is used twice. Kuhn&apos;s algorithm repeatedly searches for augmenting paths that grow the matching. It underlies job assignment, kidney-exchange programs, and ad allocation, and is the combinatorial core of many scheduling systems."><BipartiteMatchingStudio /></StudioPageShell>; }
