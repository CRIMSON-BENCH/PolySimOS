import type { Metadata } from "next";
import { CriticalPathStudio } from "@/components/studio/CriticalPathStudio";
import { StudioPageShell } from "@/components/studio/StudioPageShell";
export const metadata: Metadata = { title: "Critical Path Method (CPM) Simulator (Browser)", description: "Find a project's critical path and minimum duration from task dependencies. Adjust durations and watch it re-route. Free.", alternates: { canonical: "/studio/critical-path" } };
export default function Page() { return <StudioPageShell slug="critical-path" name="Critical Path Method" keyword="critical path method CPM" lede="Which tasks actually control your deadline? The critical path is the longest chain of dependencies — delay anything on it and the whole project slips." about="The Critical Path Method computes the earliest and latest start of every task from the dependency network, then identifies the sequence with zero slack: the critical path. Its length is the minimum project duration. Tasks off the path have float and can slip without harm. Adjusting a task&apos;s duration can re-route the critical path entirely — the core of project scheduling."><CriticalPathStudio /></StudioPageShell>; }
