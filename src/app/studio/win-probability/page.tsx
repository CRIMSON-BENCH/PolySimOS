import type { Metadata } from "next";
import { WinProbabilityStudio } from "@/components/studio/WinProbabilityStudio";
import { StudioPageShell } from "@/components/studio/StudioPageShell";
export const metadata: Metadata = { title: "Live Win Probability Simulator (Sports, Browser)", description: "See how win probability depends on the lead and time remaining, producing the live curve broadcasts show. Free, interactive.", alternates: { canonical: "/studio/win-probability" } };
export default function Page() { return <StudioPageShell slug="win-probability" name="Live Win Probability" keyword="win probability sports model" lede="A 10-point lead means very different things in the first quarter and the final minute. Win probability turns score and clock into a single live percentage." about="Win-probability models treat the remaining scoring margin as a random variable whose uncertainty shrinks as the clock runs down. Early, even a large lead is fragile because much scoring remains; late, the same lead is nearly decisive. The resulting curve is the win-probability graph now standard on sports broadcasts and betting screens."><WinProbabilityStudio /></StudioPageShell>; }
