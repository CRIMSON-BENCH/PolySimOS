import type { Metadata } from "next";
import { MarathonPacingStudio } from "@/components/studio/MarathonPacingStudio";
import { StudioPageShell } from "@/components/studio/StudioPageShell";
export const metadata: Metadata = { title: "Marathon Pace Calculator (Splits & Strategy, Browser)", description: "Compute race pace, splits, and even/negative/positive split strategies for 10K, half, and full marathon. Free, interactive.", alternates: { canonical: "/studio/marathon-pacing" } };
export default function Page() { return <StudioPageShell slug="marathon-pacing" name="Marathon Pacing" keyword="marathon pace calculator" lede="Miss your goal pace by seconds a kilometer and you miss it by minutes at the finish. Plan your splits — and the strategy that avoids hitting the wall." about="Race pace is the target time divided by the distance, but distribution decides the outcome. A negative split — starting controlled and finishing faster — is how nearly every marathon record is set, because going out too hard depletes glycogen and triggers the wall. This tool lays out per-kilometer splits for even, negative, and positive pacing."><MarathonPacingStudio /></StudioPageShell>; }
