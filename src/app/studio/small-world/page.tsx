import type { Metadata } from "next";
import { SmallWorldStudio } from "@/components/studio/SmallWorldStudio";
import { StudioPageShell } from "@/components/studio/StudioPageShell";
export const metadata: Metadata = { title: "Small-World Network Simulator (Watts-Strogatz, Browser)", description: "Explore the Watts-Strogatz small-world model — how a few rewired links create six degrees of separation. Free, interactive.", alternates: { canonical: "/studio/small-world" } };
export default function Page() { return <StudioPageShell slug="small-world" name="Small-World Network" keyword="small-world network Watts-Strogatz" lede="Six degrees of separation, explained. A few random shortcuts turn an orderly network into one where everyone is surprisingly close — while staying locally clustered." about="The Watts-Strogatz model starts from a ring lattice and rewires each edge with probability p. Even a small p adds long-range shortcuts that collapse the average path length dramatically, while local clustering stays high. This small-world regime describes social networks, neural wiring, and power grids, and explains why rumors and epidemics spread so fast."><SmallWorldStudio /></StudioPageShell>; }
