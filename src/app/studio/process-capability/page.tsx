import type { Metadata } from "next";
import { ProcessCapabilityStudio } from "@/components/studio/ProcessCapabilityStudio";
import { StudioPageShell } from "@/components/studio/StudioPageShell";
export const metadata: Metadata = { title: "Process Capability Calculator (Cp, Cpk, Browser)", description: "Compute Cp, Cpk, defect rate, and sigma level from process mean, spread, and spec limits. Free, interactive quality tool.", alternates: { canonical: "/studio/process-capability" } };
export default function Page() { return <StudioPageShell slug="process-capability" name="Process Capability (Cp / Cpk)" keyword="process capability Cp Cpk" lede="Can your process actually hold the tolerances? Cp and Cpk turn that question into numbers — and reveal how fast defects climb when it drifts off-center." about="Cp compares the specification width to the process spread, and Cpk also accounts for centering. A Cpk of 1.33 is the common bar for a capable process; 2.0 is Six Sigma, allowing only 3.4 defects per million. Widening the spread or shifting the mean toward a spec limit rapidly increases the defect rate — the heart of statistical quality control."><ProcessCapabilityStudio /></StudioPageShell>; }
