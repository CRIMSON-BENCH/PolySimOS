import type { Metadata } from "next";
import { ControlChartStudio } from "@/components/studio/ControlChartStudio";
import { StudioPageShell } from "@/components/studio/StudioPageShell";
export const metadata: Metadata = { title: "SPC Control Chart Simulator (X-bar, Browser)", description: "Watch a statistical process control chart flag out-of-control points against ±3σ limits as the process shifts. Free, interactive.", alternates: { canonical: "/studio/control-chart" } };
export default function Page() { return <StudioPageShell slug="control-chart" name="SPC Control Chart" keyword="SPC control chart" lede="How do you know when a process has really changed versus just wobbled? Statistical process control draws the line at three sigma — and watches for breaches." about="A control chart plots a process measurement over time against a center line and control limits set at three standard deviations. Points within the limits reflect normal random variation and should be left alone; a point beyond them, or a sustained run, signals a genuine shift worth investigating. Nudge the process and watch points breach the limits — the foundation of quality monitoring."><ControlChartStudio /></StudioPageShell>; }
