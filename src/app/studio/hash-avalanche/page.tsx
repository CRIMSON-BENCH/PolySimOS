import type { Metadata } from "next";
import { HashAvalancheStudio } from "@/components/studio/HashAvalancheStudio";
import { StudioPageShell } from "@/components/studio/StudioPageShell";
export const metadata: Metadata = { title: "Hash Function Avalanche Effect Simulator (Browser)", description: "See the avalanche effect — how flipping one input bit changes about half the output bits of a hash. Free, interactive.", alternates: { canonical: "/studio/hash-avalanche" } };
export default function Page() { return <StudioPageShell slug="hash-avalanche" name="Hash Avalanche Effect" keyword="hash avalanche effect" lede="Change one letter and the entire fingerprint scrambles. The avalanche effect is what makes cryptographic hashes trustworthy — see it bit by bit." about="A cryptographic hash maps any input to a fixed-size digest, and a well-designed one exhibits the avalanche effect: flipping a single input bit flips roughly half the output bits, unpredictably. This decorrelation from the input is what lets hashes secure passwords, verify file integrity, and anchor digital signatures and blockchains. This demo uses a small toy hash to make the effect visible."><HashAvalancheStudio /></StudioPageShell>; }
