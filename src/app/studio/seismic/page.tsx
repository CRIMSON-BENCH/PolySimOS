import type { Metadata } from "next";
import { SeismicStudio } from "@/components/studio/SeismicStudio";
import { StudioPageShell } from "@/components/studio/StudioPageShell";
export const metadata: Metadata = { title: "Earthquake Magnitude, Energy & Shaking Calculator", description: "Convert earthquake magnitude to energy, TNT equivalent, and estimate peak ground acceleration and Mercalli intensity by distance. Free.", alternates: { canonical: "/studio/seismic" } };
export default function Page() { return <StudioPageShell slug="seismic" name="Earthquake Magnitude & Shaking" keyword="earthquake magnitude energy calculator" lede="A magnitude 7 is not twice a magnitude 6 — it releases about 32 times more energy. See what the magnitude scale really means and how shaking fades with distance." about="The moment magnitude scale is logarithmic in ground-motion amplitude, so energy grows as roughly 10^(1.5·M) — each whole step is about a 32-fold jump. Shaking attenuates with distance from the epicenter; peak ground acceleration maps to the Modified Mercalli intensity that people and buildings experience. A conceptual tool, not a site-specific seismic hazard assessment."><SeismicStudio /></StudioPageShell>; }
