import type { Metadata } from "next";
import { HaversineStudio } from "@/components/studio/HaversineStudio";
import { StudioPageShell } from "@/components/studio/StudioPageShell";
export const metadata: Metadata = { title: "Great-Circle Distance Calculator (Haversine, Browser)", description: "Compute the great-circle distance and bearing between two latitude/longitude points with the haversine formula. Free, interactive.", alternates: { canonical: "/studio/haversine" } };
export default function Page() { return <StudioPageShell slug="haversine" name="Great-Circle Distance (Haversine)" keyword="haversine great-circle distance" lede="What is the real distance between two cities? Not the straight line on a flat map — the great-circle arc across the curved Earth, which the haversine formula nails." about="The shortest path between two points on a sphere is a great-circle arc. The haversine formula computes its length from the two points&apos; latitude and longitude, remaining numerically stable even for nearly antipodal points. It underlies flight planning, GPS, ride-hailing distances, and any &quot;how far apart&quot; calculation on Earth."><HaversineStudio /></StudioPageShell>; }
