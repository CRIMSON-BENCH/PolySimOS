import type { Metadata } from "next";
import { EllipticCurveStudio } from "@/components/studio/EllipticCurveStudio";
import { StudioPageShell } from "@/components/studio/StudioPageShell";
export const metadata: Metadata = { title: "Elliptic Curve Point Addition Simulator (ECC, Browser)", description: "See the geometric point-addition rule on an elliptic curve — the foundation of elliptic-curve cryptography. Free, interactive.", alternates: { canonical: "/studio/elliptic-curve" } };
export default function Page() { return <StudioPageShell slug="elliptic-curve" name="Elliptic Curve Addition" keyword="elliptic curve cryptography addition" lede="Modern cryptography runs on a strange kind of arithmetic where you add points on a curve. See the elegant geometric rule that secures Bitcoin and TLS." about="On an elliptic curve y² = x³ + ax + b, adding two points means drawing the line through them, finding the third intersection with the curve, and reflecting it over the x-axis. Repeating this scalar multiplication is fast, but undoing it — the elliptic-curve discrete logarithm — is infeasible. That asymmetry gives ECC RSA-grade security with far smaller keys, ideal for phones and embedded devices."><EllipticCurveStudio /></StudioPageShell>; }
