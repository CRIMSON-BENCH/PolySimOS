import type { Metadata } from "next";
import { GraphTraversalStudio } from "@/components/studio/GraphTraversalStudio";
import { StudioPageShell } from "@/components/studio/StudioPageShell";
export const metadata: Metadata = { title: "BFS vs DFS Graph Traversal Visualizer (Browser)", description: "Watch breadth-first and depth-first search explore a graph in different orders, using a queue vs a stack. Free, interactive.", alternates: { canonical: "/studio/graph-traversal" } };
export default function Page() { return <StudioPageShell slug="graph-traversal" name="Graph Traversal (BFS / DFS)" keyword="BFS DFS graph traversal" lede="Two fundamental ways to explore any graph. Breadth-first fans out level by level; depth-first plunges deep and backtracks. Watch the visit order diverge." about="Breadth-first search uses a queue to visit all nearby nodes before far ones, making it perfect for shortest paths and level-order processing. Depth-first search uses a stack to dive as deep as possible before backtracking, ideal for cycle detection, topological sorting, and maze solving. The numbered visit order shows exactly how the two strategies differ."><GraphTraversalStudio /></StudioPageShell>; }
